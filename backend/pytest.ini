[pytest]
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
testpaths = tests
markers =
    integration: requires live Postgres/Redis/Qdrant (set CREATORRAG_INTEGRATION=1)
